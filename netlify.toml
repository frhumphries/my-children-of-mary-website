[build]
  publish = "."
  command = ""

[forms]
  # Netlify Forms are enabled automatically when forms have data-netlify="true"

[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"